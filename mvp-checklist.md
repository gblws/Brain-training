首页能进入 3 个游戏页
舒尔特：能开始、计时、结束后弹结果、能写入历史
斯特鲁普：20 轮结束、计算正确率、能写入历史
记忆矩阵：关卡推进/失败重开正常、通关后能写入历史
历史页：能拉取数据、空数据不报错
后端挂掉时：前端有错误提示、不崩溃

后端接口测试：
GET /api/v1/user/history 返回 code=200 且 data 是数组
POST /api/v1/game/submit 返回成功，且数据库确实新增一条记录